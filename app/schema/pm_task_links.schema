CREATE TABLE %s (
	id BIGINT(20) NOT NULL AUTO_INCREMENT,
	project_id BIGINT(20) NOT NULL,
	source_task_id BIGINT(20) NOT NULL,
	target_task_id BIGINT(20) NOT NULL,
	type VARCHAR(100) NOT NULL,
	PRIMARY KEY  (id)
) CHARACTER SET utf8 COLLATE utf8_general_ci;